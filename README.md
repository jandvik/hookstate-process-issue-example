# hookstate-process-issue-example
Example project for https://github.com/avkonst/hookstate/issues/118

Start Dev Server:
`yarn run start:dev`

Build Prod:
`yarn run build:prod`

Serve Prod Build:
`yarn run start`
